// src/environments/environment.ts
export const environment = {
  production: false,
  // Si quieres usar el backend de Render también en dev, deja esto:
  apiBaseUrl: 'https://filatelia.onrender.com',
  // Si quisieras usar back local, sería: 'http://localhost:3000'
};
